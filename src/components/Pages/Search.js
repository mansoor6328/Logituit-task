import MotionHoc from "./MotionHoc";

const SearchComponent = () => {
  // eslint-disable-next-line jsx-a11y/heading-has-content
  return <h1></h1>;
};

const Search = MotionHoc(SearchComponent);

export default Search;
